apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: piums

resources:
  - namespace.yaml
  - configmap.yaml
  - secrets.yaml
  - deployments.yaml
  - services.yaml
  - ingress.yaml
  - hpa.yaml

commonLabels:
  app.kubernetes.io/part-of: piums-platform
  app.kubernetes.io/managed-by: kustomize

# Terraform
.terraform/
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.*
crash.log
crash.*.log
# Nunca commitear valores reales
terraform.tfvars
*.auto.tfvars
override.tf
override.tf.json
# Los planes guardados y los logs de apply llevan valores resueltos (passwords,
# hosts, tokens) en claro. Igual de sensibles que el tfstate.
*.plan
tf.plan
apply.log
*.apply.log
# El lock de providers SÍ se commitea (.terraform.lock.hcl) para pinnear versiones.

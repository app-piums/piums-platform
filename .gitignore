# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Next.js
.next/
out/
build/
dist/

# Production
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables
.env
.env*.local
.env.staging
.env.production
.env.development

# Backup files (pueden contener credenciales)
*.bak
*.backup

# Firebase service account JSONs
*firebase-adminsdk*.json
*serviceaccount*.json

# Kubernetes secrets (never commit real credentials)
infra/k8s/overlays/**/*secret*.yaml
infra/k8s/overlays/**/*secrets*.yaml
# ...pero las plantillas SI se versionan: el runbook y el workflow de deploy las
# piden por ruta y un clon limpio se quedaba sin ellas.
!infra/k8s/overlays/**/*secret*.example.yaml
!infra/k8s/overlays/**/*secrets*.example.yaml
!infra/k8s/overlays/**/*.example.yaml

# OS
.DS_Store
*.pem

# Debug
*.tsbuildinfo
next-env.d.ts

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Turbo
.turbo

# Avoid nested git repos
piums-platform/

# Seguridad: nunca commitear credenciales ni temporales
credenciales-demo.md
.fuse_hidden*
_tmp_*

# Dumps de la base: pesan y traen datos de usuarios reales.
# Los dumps ya versionados siguen trackeados (.gitignore no destrackea lo que ya
# esta en el indice); esto solo evita subir nuevos sin querer con `git add .`.
backups/*.sql
